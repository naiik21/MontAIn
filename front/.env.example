# Copiar a front/.env. Astro solo expone al cliente las variables con
# prefijo PUBLIC_.

# URL base de la API, sin barra final. Sin definir, apunta a localhost:8000.
# En Vercel se configura en Settings -> Environment Variables.
# PUBLIC_API_URL=https://montain-api.onrender.com
