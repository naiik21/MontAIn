# Dependencias de la API (main.py). Versiones fijadas para despliegue reproducible.
# Para entrenar los modelos, ver training/requirements.txt

# Servidor
fastapi==0.128.1
uvicorn[standard]==0.40.0
python-multipart==0.0.22
python-dotenv==1.2.1

# Analisis de rutas
gpxpy==1.6.2
SRTM.py==0.3.7
geopy==2.4.1
numpy==2.3.5
pandas==2.3.3
# Variante sin GPU: se importa igual (`import xgboost`) y carga los modelos
# entrenados con el paquete normal. El xgboost completo arrastra 400 MB de
# librerias CUDA que no sirven de nada en un despliegue sin GPU.
xgboost-cpu==3.1.3

# Mapa
folium==0.20.0

# Descripcion generada por IA
anthropic==0.79.0

# No lo importa nuestro codigo: lo necesitan SRTM.py para bajar los tiles de
# elevacion y folium para sus recursos. Se fija aqui para que el despliegue
# sea reproducible en vez de depender de la version que resuelva pip.
requests==2.32.5
